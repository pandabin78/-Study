# 06.10 수업 내용 정리

UI (User Interface) - 유저와 상호작용 
- UI -> GUI => OnGUI(옜날 방싱) => UGUI => Unity UI(디자이너 영역에 가깝다. 실 회사에서 잘 사용되진 않는다.)\
게임 쪽에서 복잡하게 사용된다.

UX(User Expire 익스피리언스)

GameObjet => Canvas - UI를 넣은 공간
Transform => RectTransform - 크기가 화면가 1:1비율이다.

canvas 계층 구조
```
- cancas - (Overay(화면 앞에), cammera(카메라 앞에), world(게임속 움직이는 체력바 같은 것))
    - panel 
        - Image
        - RawImager
        - Button
            - Image
            - Text
```

앵커 - 기준\
피벗 - 중심점

stretch - 앵커 

L T R B - 피벗 - 가변

- Anchors - 부모로부터의 기준
    - stretch : 가변 - Left, Top, Right, Bottom
    - Fixed : 고정 - PoxX, PoxY, Width, Height
- pivot - 자신의 기준
   
- Anchor로 부터 Pivot까지의 거리
    - PoxX, PoxY