A방향 * B방향 * Cos = (A.x * B.x) + (A.y * B.y)
정규화(Normalize) => 단위 백터(Unit) => 1인 백터
1 * 1 * cos = acos(빗변분의 빝변)

두 벡터 =>  직각	=> 90` = 0
		   일치	=> 180	 ┐ => -1
				=> -180	 ┘

내적 (Dot) A * B, A / B
외적 (cross) A * B, A ^ B
```csharp
horizontalInput = Input.GetAxisRaw("Horizontal"); //즉각 반응, -1.0f(왼쪽), 0.0f(정지), 1.0f(오른쪽)
animator.SetBool("bRun", Mathf.Abs(horizontalInput) > Mathf.Epsilon); // Epsilon: 0.00001f(1e-6f) float 형의 작은 수
```