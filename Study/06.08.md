# 06.08 수업 내용 정리

컨트롤 f 스크립트 하나에서 찾기 \
컨트롤 h 스크립트 하나에서 바꾸기

컨트롤 쉬프트 f 스크립트 전체에서 찾기 \
컨트로 쉬프트 h 스크립트 전체에서 바꾸기

Tag 는 GameObjet에 저장된다. \
CompareTag 테그 찾기
(collision.gameObject.CompareTag("Player") == false)

카메라 범위찾기 \
Camera camera = Camera.main.GetComponent<Camera>();
        camerSize = camera.orthographicSize;