# 06.03 수업 내용 정리

#### enabled :
>컴포넌트 O/F,  \
ture = on \
false = off

#### ForceMode2D. Force, Impulse
>Force = 지속적인 힘(박스를 밀 때), \
Impulse = 한번에 힘(박스를 던질 때)

#### Invoke :	Invoke("함수", 3.0f);
>함수를 일정 시간 이후에 작동하게 해줌 \
실무에선 사용하진 않음.

#### 게임 종료
>UnityEditor.EditorApplication.isPlaying = false;\
Application.Quit();